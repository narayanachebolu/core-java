package org.com.designpatterns.singleton;

public class CloneTest {
    public static void main(String[] args) {
        Singleton singletonInstance = Singleton.INSTANCE;

        //singletonInstance.clone();
    }
}
